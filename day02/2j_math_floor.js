/*Math.round(2.8); // 3   (nearest)
Math.floor(2.8); // 2   (always down)
Math.ceil(2.8);  // 3  (always up)

2j. Let's say we want to always round a number down (2.8 => 2)
*/

let number=22.8;
console.log("Before Math.floor()");
console.log(number);
console.log("after Math.floor()");
console.log(Math.floor(number));
